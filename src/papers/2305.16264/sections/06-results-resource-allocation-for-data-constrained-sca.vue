<!-- Results: Resource Allocation for Data-Constrained Scaling -->
<script setup lang="ts">
import { Heading, Para, En, Zh, Figure } from '../../../components/paper'
</script>

<template>
<Heading :level="1" label="sec:fixc" en="Results: Resource Allocation for Data-Constrained Scaling" zh="结果：数据受限扩展的资源分配" />

<Figure src="figures/2305.16264/isoloss_100m.svg" :width="100" label="fig:100misoloss" caption-en="\textbf{IsoLoss contours for 100 million unique tokens.} \textit{(Left):} 93 models trained with varying parameters and epochs on a fixed dataset. Contours show an interpolation of results with the same final test loss. \textit{(Right):} Comparison with the loss predictions from our proposed scaling laws for the same budget of 100 million unique tokens and the predicted efficient frontier. The diminishing returns from training on repeated data can be seen in the increase in distance of the contour curves." caption-zh="\textbf{1 亿唯一词元的等损失（IsoLoss）等值线。}\textit{（左）：}在固定数据集上以不同参数量与 epoch 数训练的 93 个模型。等值线显示相同最终测试损失结果的插值。\textit{（右）：}与我们所提缩放定律在同一 1 亿唯一词元预算下的损失预测以及预测的有效前沿的比较。重复数据训练的回报递减可以从等值线间距的增大中看出。" />

<Para>
  <En>Our first experimental setting considers scaling in a setting where all models have the same data constraint. For these experiments,  the unique training data budget $D_C$ is fixed at either 100M, 400M or 1.5B tokens. For each data budget, we train a set of language models with increasing amounts of compute that is allocated to either more parameters or more epochs on the unique training data.</En>
  <Zh>我们的第一个实验设定考虑所有模型具有相同数据约束时的扩展。 在这些实验中，唯一训练数据预算 $D_C$ 固定为 1 亿（100M）、4 亿（400M）或 15 亿（1.5B）词元。对每个数据预算，我们训练一组计算量递增的语言模型，新增计算或用于增加参数，或用于在唯一训练数据上运行更多 epoch。</Zh>
</Para>

<Para>
  <En>\autoref{fig:100misoloss} (left) shows the main results for scaling with 100M unique tokens\footnote{Although small, for example, this is the order of magnitude of a realistic data constraint reflecting data available after filtering the OSCAR dataset~\citep{OrtizSuarezSagotRomary2019} for Basque, Punjabi, or Slovenian.} (see \autoref{sec:addcon} for 400M and 1.5B tokens). For 100M tokens, the corresponding one-epoch compute-optimal model according to scaling laws from~\cite{hoffmann2022training} has $U_N$ of approximately 7M parameters (see \autoref{sec:c4scaling} for the scaling coefficients we use). Results show that more than a 50\% reduction in loss can be attained by training for several epochs ($R_D>0$) and increasing model size beyond what would be compute-optimal for 100M tokens ($R_N>0$). We find the best loss to be at around 20-60$\times$ more parameters and epochs, which corresponds to spending around 7000$\times$ more FLOPs. These results suggest that one-epoch models significantly under-utilize their training data and more signal can be extracted by repeating data and adding parameters at the cost of sub-optimal compute utilization.</En>
  <Zh>\autoref{fig:100misoloss}（左）展示了在 1 亿唯一词元下扩展的主要结果\footnote{虽然规模不大，例如，这大致相当于对 OSCAR 数据集~\citep{OrtizSuarezSagotRomary2019} 中巴斯克语、旁遮普语或斯洛文尼亚语数据过滤后所得数据量的数量级，是一种现实的数据约束。}（4 亿与 15 亿词元的结果见 \autoref{sec:addcon}）。对 1 亿词元而言，按照~\cite{hoffmann2022training} 的缩放定律，对应的单 epoch 计算最优模型的 $U_N$ 约为 700 万参数（我们使用的缩放系数见 \autoref{sec:c4scaling}）。 结果表明，通过训练多个 epoch（$R_D>0$）并把模型规模扩大到超出 1 亿词元的计算最优值（$R_N>0$），损失可以降低 50\% 以上。我们发现最佳损失出现在参数量与 epoch 数均增大约 20-60$\times$ 附近，这相当于多花费约 7000$\times$ 的 FLOPs。这些结果表明，单 epoch 模型显著未充分利用其训练数据；通过重复数据并增大参数量可以提取更多信号，代价是计算利用率次优。</Zh>
</Para>

<Para>
  <En>\autoref{fig:100misoloss} (right) shows the predicted contours created by fitting our data-constrained scaling laws on 182 training runs. In the single-epoch case ($R_D=0$) with near compute-optimal parameters ($R_N=0$) our scaling equation (\autoref{sec:parametricfit}) reduces to the Chinchilla equation. In this case, both formulas predict the optimal allocation of compute to parameters and data to be the same, resulting in overlapping efficient frontiers. As data is repeated for more than a single epoch, our fit predicts that excess parameters decay faster in value than repeated data ($R_N^*&lt;R_D^*$). As a result, the data-constrained efficient frontier suggests allocating most additional compute to more epochs rather than more parameters. This contrasts the Chinchilla scaling laws~\cite{hoffmann2022training}, which suggest equally scaling both. However, note that they do not repeat the entire training data and their parametric fit explicitly relies on the assumption that models are trained for a single epoch only. Thus, there is no guarantee that their scaling predictions hold for repeated data.</En>
  <Zh>\autoref{fig:100misoloss}（右）展示了在 182 次训练运行上拟合我们的数据受限缩放定律得到的预测等值线。在单 epoch（$R_D=0$）且参数接近计算最优（$R_N=0$）的情形下，我们的缩放方程（\autoref{sec:parametricfit}）退化为 Chinchilla 方程。此时两个公式对计算在参数与数据之间的最优分配给出相同预测，二者的有效前沿相互重合。当数据重复超过单个 epoch 时，我们的拟合预测过剩参数的价值比重复数据衰减得更快（$R_N^*&lt;R_D^*$）。因此，数据受限的有效前沿建议把大部分新增计算分配给更多 epoch 而非更多参数。这与 Chinchilla 缩放定律~\cite{hoffmann2022training}形成对比，后者建议二者等比例扩展。但请注意，他们并未重复全部训练数据，且其参数化拟合明确依赖于模型只训练单个 epoch 的假设。因此无法保证其缩放预测在重复数据下仍然成立。</Zh>
</Para>

<Figure src="figures/2305.16264/validation_c4.svg" :width="100" label="fig:validation" caption-en="\textbf{Validation Loss for Different Data Constraints (IsoFLOP).} Each curve represents the same number of FLOPs spent on an equal-sized model. Colors represent different numbers of epochs due to repeating because of data constraints. Parameters and training tokens are set to match the single-epoch compute-optimal configurations for the given FLOPs. Models trained on data that is repeated for multiple epochs have consistently worse loss and diverge if too many epochs are used. Only loss curves for 8.7B runs are smoothed with an exponential moving average and weight of 0.85." caption-zh="\textbf{不同数据约束下的验证损失（IsoFLOP）。}每条曲线代表花费相同 FLOPs、模型规模相同的结果。颜色表示因数据约束而重复数据所产生的不同 epoch 数。参数量与训练词元数按给定 FLOPs 的单 epoch 计算最优配置设置。在重复多个 epoch 的数据上训练的模型损失始终更差，epoch 过多时会发散。只有 8.7B 运行的损失曲线经权重 0.85 的指数移动平均平滑。" />

<Para>
  <En>For all three data budgets, our results suggest that \emph{Allocation} is optimized by scaling epochs faster than parameters. We confirm this at scale by training the data-constrained compute-optimal model for $9.3 \times 10^{21}$ FLOPs and 25 billion unique tokens as suggested by our efficient frontier. Despite having 27\% fewer parameters, this model achieves better loss and downstream performance than the model suggested by the Chinchilla scaling laws (\autoref{fig:returnalloc} (right) and \autoref{tab:8b7rep}). Similarly, the 120 billion parameter Galactica model trained on repeated data should have been significantly smaller according to data-constrained scaling laws (\autoref{sec:galactica}). An additional benefit of using a smaller model is cheaper inference, though adding parameters can make it easier to parallelize training across GPUs.</En>
  <Zh>对全部三个数据预算，我们的结果都表明，让 epoch 的扩展快于参数的扩展可以优化\emph{分配}。为在大规模下验证这一点，我们按照有效前沿的建议，以 $9.3 \times 10^{21}$ FLOPs 和 250 亿唯一词元训练了数据受限的计算最优模型。尽管参数量少 27\%，该模型仍取得了比 Chinchilla 缩放定律所建议模型更好的损失与下游性能（\autoref{fig:returnalloc}（右）与 \autoref{tab:8b7rep}）。类似地，在重复数据上训练的 1200 亿参数 Galactica 模型，按照数据受限缩放定律本应显著更小（\autoref{sec:galactica}）。使用较小模型的另一个好处是推理成本更低，不过增加参数可以让训练更容易在多个 GPU 之间并行。</Zh>
</Para>

<Para>
  <En>Adding parameters and epochs causes the loss to decrease and eventually increase again, suggesting that too much compute can hurt performance. Results from \cite{kaplan2020scaling} also show that loss can increase when too many parameters are used, even with early stopping. However, we expect that appropriate regularization (such as simply removing all excess parameters as an extreme case) could prevent this behavior. Thus, our formula presented in \autoref{sec:method} and its predicted isoLoss contours in \autoref{fig:100misoloss} do not model the possibility that excess epochs or parameters could hurt performance.</En>
  <Zh>增加参数与 epoch 会使损失先降后升，这表明过多的计算可能损害性能。\cite{kaplan2020scaling} 的结果也表明，即使使用早停，参数过多时损失也可能上升。不过我们预计，适当的正则化（极端情形如直接移除全部过剩参数）可以避免这种行为。因此，\autoref{sec:method} 中给出的公式及其在 \autoref{fig:100misoloss} 中预测的等损失等值线，并未对过剩 epoch 或参数可能损害性能的情形建模。</Zh>
</Para>
</template>
